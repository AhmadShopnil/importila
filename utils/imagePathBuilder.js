
import { BASE_URL } from "./baseUrl"

export const imagePathBuilder=(path)=>{
    return `${BASE_URL}${path}`
}

// export const imagePathBuilder=(path)=>{
//     return `http://localhost:3000${path}`
// }
